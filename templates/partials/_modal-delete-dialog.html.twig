<div class="modal fade" id="confirmDeleteModal" tabindex="-1" aria-labelledby="confirmDeleteModal" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
			</div>
			<div class="modal-body">
				<input type="hidden" name="href" id="href" value=""/>
				<p>{{ 'app.confirmation.label'|trans }}</p>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-light custom-btn bi bi-x-circle" data-bs-dismiss="modal">{{ 'app.no.label'|trans }}</button>
				<button type="button" id="confirmDelete" class="btn btn-primary custom-btn bi bi-floppy2">{{ 'app.yes.label'|trans }}</button>
			</div>
		</div>
	</div>
</div>
